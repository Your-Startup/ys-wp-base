core download:
  locale: ru_RU
  skip-content:
core config:
  dbname: ys
  dbuser: root
  dbpass:
  extra-php: |
    define('WP_DEBUG', true);
    define('WP_DEBUG_LOG', true);
    define('WP_DEBUG_DISPLAY', false);
core install:
  url: http://localhost/ys/
  title: ys
  admin_user: ys
  admin_password: ysadmin
  admin_email: admin@example.dev
